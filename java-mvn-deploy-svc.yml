apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-maven-app
  labels:
     app: java-maven-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-maven-app
  template:
    metadata:
      labels:
        app: java-maven-app
    spec:
      containers:
      - name: sample-ctr
        image: devopstrainer/java-mvn-privaterepo:2
        ports:
        - containerPort: 8080
apiVersion: v1
kind: Service
metadata:
  labels: 
      app: java-maven-app
spec:
  selector:
    app: java-maven-app
  ports:
  - port: 8080
    nodePort: 30001
    protocol: TCP
  type: NodePort
